#macro(info $title, $message)
<div class="alert alert-info">
    <span class="glyphicon glyphicon-info-sign"></span> <strong>${title}</strong>
    <hr class="message-inner-separator">
    <p>${message}</p>
</div>
#end